/* balance_lib.c */
unsigned char *balance_u8(unsigned char *data, size_t size, size_t nb_min, size_t nb_max);
unsigned char *balance_intensity_u8(unsigned char *data, size_t size, size_t nb_min, size_t nb_max);
